# Security Policy

## Supported Versions
Only the latest branch (`main`) is supported for security updates.

## Reporting a Vulnerability
If you discover a security vulnerability within this project, please do not open a public issue. Instead, reach out privately via LinkedIn or email.

## Security Considerations
* No sensitive credentials are kept in the frontend code.
* Environment variables (`.env`) are correctly excluded via `.gitignore`.
